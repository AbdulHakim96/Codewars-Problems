function calcType(a, b, res) {
 
  let add = a+b ;
  let mult = a*b;
  let div = a/b;
  let sub = a-b;
  
  if(res === add) return 'addition'
  if(res === mult) return 'multiplication'
  if(res === div) return 'division'
  if(res === sub) return 'subtraction'
 
}
